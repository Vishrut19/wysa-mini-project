import { Home, Goals, Success } from '../components';

const componentArray = [Home, Goals, Success];

const onboardingElements = {
  elements: componentArray,
  numberOfElements: componentArray.length,
};

export default onboardingElements;
